from setuptools import setup, find_packages

setup(
    name="backend-olimpiadas",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Suas dependências base aqui
    ],
)